<div class="fab-container left">
  <button class="fab-main" id="fabMain">☰</button>

  <div class="fab-options" id="fabOptions">
    <button class="fab-option" id="scrollTopBtn">&uarr;</button>
    <button class="fab-option" id="menuBtn"><i class='bx  bx-sidebar'></i></button>
    <button class="fab-option" onclick="openCart()"><i class="bx bx-home"></i></button>
    <button class="fab-option" onclick="window.location.href='/contact'">
      <i class="bx bx-phone-outgoing bx-tada"></i>
    </button>
  </div>
</div>
 
<div class="sidebar closed" id="sidebar">
  <h2 id="userEmail"></h2>  
  <ul>
    <li><a class="active" href="/about"> <i class='bx  bx-book-content'></i>  About site</a></li> 
    <li><a href="#"><i class='bx bx-purchase-tag icon'></i> Discounts</a></li>
    <li><a href="/save"> <i class='bx  bx-bookmark-heart'></i>  Save</a></li>
    <li><a href="/register"><i class='bx  bx-book-open'></i> Registration</a></li>
    <li><a href="/logout" class="logout-btn"><i class='bx  bx-user'></i> Logout</a></li>
  </ul>
        
        <div class="ad-panel">
          <div class="ad-content">
            <img src="/img" alt="Ad">
          </div>
        </div>
 


    
</div>
 
<div class="content" id="content"></div> 
<script>
(function () {
  const fabMain = document.getElementById("fabMain");
  const fabOptions = document.getElementById("fabOptions");

  document.addEventListener("click", (e) => {
    if (e.target.id === "fabMain") {
      fabOptions.classList.toggle("active");
      fabMain.textContent = fabOptions.classList.contains("active") ? "✖" : "☰";
    }

    if (e.target.id === "scrollTopBtn") {
      smoothScrollToTop();
    }
  });

  function smoothScrollToTop() {
    const duration = 600;
    const start = window.scrollY;
    const startTime = performance.now();

    function animateScroll(currentTime) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const ease = 1 - Math.pow(1 - progress, 3);
      window.scrollTo(0, start * (1 - ease));
      if (progress < 1) requestAnimationFrame(animateScroll);
    }

    requestAnimationFrame(animateScroll);
  }

  const fabContainer = document.querySelector(".fab-container");
  window.addEventListener("scroll", () => {
    fabContainer.style.display = window.scrollY > 200 ? "flex" : "none";
  });
})();

function openCart() {
  window.location.href = "/skeleton";
}
 
const menuBtn = document.getElementById("menuBtn");
const sidebar = document.getElementById("sidebar");
const content = document.getElementById("content");

menuBtn.addEventListener("click", () => {
  sidebar.classList.toggle("closed");
  content.classList.toggle("full");
});
</script>
